# Example environment configuration for coinbase-logger

# Data storage path on the host system
# This directory will store all the binary market data files
# Default: ./data (relative to docker-compose.yml)
DATA_PATH=./data

# Optional: Timezone configuration
# Default: UTC
TZ=UTC